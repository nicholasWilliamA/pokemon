import { View, Text } from "react-native";
import { PokemonDetailProps } from "../../components/PokemonDetail";

const PokemonStats: React.FC<PokemonDetailProps> = ({ navigation, route, data }) => {
    return (
        <Text>Hello</Text>
    );

};

export default PokemonStats;
